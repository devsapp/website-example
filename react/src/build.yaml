start_react:
  defaultBuilder:
    languages:
      - nodejs20
    steps:
      - run: npm install && npm run build
        path: ./code
      - run: cp nginx.conf ./build/nginx.conf
        path: ./code